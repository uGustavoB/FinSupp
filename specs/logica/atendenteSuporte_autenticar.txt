@startuml

start
:Receber parâmetro (senha fornecida);
:Buscar a senha armazenada do atendente atual no sistema;
:Comparar a senha fornecida com a senha armazenada;

if (As senhas coincidem?) then (Sim)
    :Retornar 'true' (autenticação bem-sucedida);
else (Não)
    :Retornar 'false' (autenticação falhou);
endif

stop

@enduml
