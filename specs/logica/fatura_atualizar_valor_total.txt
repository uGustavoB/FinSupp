@startuml
title Diagrama de Atividades: Atualizar Valor Total da Fatura

start

:Recebe parâmetros de entrada (ID da Fatura, valor da parcela);

:Valida os parâmetros de entrada;
if (Parâmetros válidos?) then (não)
  :Lança erro: "Parâmetros inválidos.";
  stop
endif

:Busca a fatura com o ID especificado;
if (Fatura existe?) then (não)
  :Lança erro: "Fatura não encontrada.";
  stop
endif

:Define o valor total da fatura como a soma do valor atual e o valor da parcela;
:Salva as alterações no banco de dados;

stop
@enduml